{
  "name": "ai-website-builder-root",
  "version": "1.0.0",
  "description": "Root package to run frontend easily",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
